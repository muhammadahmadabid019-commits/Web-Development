<div style="height: 80px;"></div>

<section class="section fade-in" style="min-height: 80vh; display: flex; align-items: center;">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6">

        <!-- Flash Messages -->
        <% if (typeof messages !== 'undefined' && messages.error && messages.error.length > 0) { %>
          <div class="error-box mb-4"><i class="fas fa-exclamation-triangle me-2"></i><%= messages.error[0] %></div>
        <% } %>
        <% if (typeof messages !== 'undefined' && messages.success && messages.success.length > 0) { %>
          <div class="success-box mb-4"><i class="fas fa-check-circle me-2"></i><%= messages.success[0] %></div>
        <% } %>

        <div class="form-box" style="text-align: left;">
          <div class="text-center mb-4">
            <i class="fas fa-user-plus" style="font-size: 2.5rem; color: #00b4ff;"></i>
            <h3 class="mt-2" style="font-weight: 800;">Create Account</h3>
            <p style="color: #7a92b0; font-size: 0.9rem;">Join SecureNet Solutions today</p>
          </div>

          <form action="/auth/register" method="POST">
            <div class="mb-3">
              <label style="color: #a8c4e0; font-size: 0.86rem; font-weight: 500;">Full Name</label>
              <input type="text" name="name" class="form-control" placeholder="Ahmad Ali" required>
            </div>
            <div class="mb-3">
              <label style="color: #a8c4e0; font-size: 0.86rem; font-weight: 500;">Email Address</label>
              <input type="email" name="email" class="form-control" placeholder="you@example.com" required>
            </div>
            <div class="mb-3">
              <label style="color: #a8c4e0; font-size: 0.86rem; font-weight: 500;">Password <span style="color: #7a92b0; font-size: 0.8rem;">(min. 6 characters)</span></label>
              <input type="password" name="password" class="form-control" placeholder="Create a strong password" minlength="6" required>
            </div>
            <div class="mb-4">
              <label style="color: #a8c4e0; font-size: 0.86rem; font-weight: 500;">Confirm Password</label>
              <input type="password" name="password2" class="form-control" placeholder="Repeat your password" required>
            </div>
            <button type="submit" class="btn btn-main w-100 py-2 fw-bold">
              <i class="fas fa-user-plus me-2"></i> Create Account
            </button>
          </form>

          <p class="text-center mt-4" style="color: #7a92b0; font-size: 0.9rem;">
            Already have an account? <a href="/auth/login" style="color: #00b4ff;">Log in here</a>
          </p>
        </div>

      </div>
    </div>
  </div>
</section>
